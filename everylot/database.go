package everylot

func (l *Lot) markAsTweeted(statusID string) {
	//         self.conn.execute("UPDATE lots SET tweeted = ? WHERE id = ?", (status_id, self.lot['id'],))
	//         self.conn.commit()
	return
}

// var query = `SELECT
//     *
//     FROM lots
//     where {} = ?
//     ORDER BY id ASC
//     LIMIT 1;
// `
